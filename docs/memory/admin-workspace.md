---
name: admin-workspace
description: "Native Admin dashboard — KPI RPC contracts, 7 alert-banner derivations, admin test account, verified live counts"
metadata: 
  node_type: memory
  type: project
  originSessionId: 5527b21c-0de8-4869-8f72-0c35b4727fce
---

**Admin workspace started 2026-07-16** in odds-app (web /admin AdminDashboard port; Dashboard tab only so far, tab strip shows ghost "SOON" chips for Sessions/New Leads/Invoice Raised). Files: [adminQueries.ts](odds-app/src/lib/adminQueries.ts), [admin.tsx](odds-app/src/screens/admin.tsx), route `admin-dashboard`, role plumbing same recipe as [[ops-workspace]] (profiles.role='admin' → Role 'admin'). Test account oddsfitnessapp@gmail.com / Oddsfitness@11 (id 2c6a0525-18d8-40aa-a5bb-df814a114452, managers=true — web gates ManagersLeaderboard on that flag; not ported yet).

**KPI RPCs** (both verified live as admin, both take `{p_range:'month'}`, IST windows server-side): `get_super_admin_active_clients_breakdown` → {activePaying, activeTraining (SAME number, two aliases), pullback, series[{label:'W1'..'W5',value}], clients[]}; pullback = eligible-tier client with no completed session in last 3 days (lifetime last session, incl. never-trained/paused). `get_super_admin_analytics` → blocks {activeClients, leads, conversions, paidSessions, qhps, workoutPlans, revenue} each {currTotal, prevTotal, data[{label,value}]}; deltaPct is CLIENT-side: `!prev ? (curr?100:0) : Math.round((curr-prev)/prev*1000)/10`. Revenue = 5 payment streams keyed on payment_date; GST 5% discount only on clients/client_renewals/misc payments when without_gst. inr formatter: ≥1e7 Cr / ≥1e5 L / ≥1e3 K.

**7 alert banners** (web components → native hooks, ALL counts verified live matching user's screenshots exactly: 27/2/21/4 + 1/1(₹2,000)/1):
- A `useAdminNewLeads`: leads stage∈(QHP Booked,Raise invoice) client_id null; QHP-Booked needs latest stage_history entry ≥ 2026-05-31 IST (fallback updated_at); Raise invoice = no cutoff; then phone-suffix(last-10)+name-token self-heal filter hides confident client matches. DIVERGENCE: native skips web's fire-and-forget client_id backfill (read-only) — same visible count.
- B `useInvoiceRaisedLeads`: stage='Raise invoice' + invoice_details + client_id NOT null, minus clients with renewal_payment_requests ilike '%"kind":"lead_invoice"%' in status awaiting_approval/awaiting_payment/failed/paid; ₹ = Σ invoice_details.amount.
- C `usePaidCancellationsPending`: session_schedule paid_cancellation=true admin_approval='pending' (head count).
- D–G `useAdminUrgentAlerts` (web useAdminClientOnboarding + AdminOnboardingUrgentAlerts): base = active clients, sub NOT null ≠'Staff', created ≥ 2026-03-04 IST (web CUTOFF — native pre-filters primary query, equivalent since all 4 alerts use newClients), paginated past 1000-cap; is_fully_onboarded = 6 stages (qhp scheduled|done, qhp done via isCompletedValue(yes/true/date-prefix), qhp_details JS-truthiness of preapproved_qhp||qhp_json — NOT SQL not-null, roster/plan/first-session RPCs get_client_*), trainer/CRM from trainer_clients actively_training + profiles.role, EXCLUDED trainer 782fe016-4ecc-4ebd-8de1-6554f2079feb (Mohit Rawat). noCrm skips Trial/Opportunity. QHP-sched alert visible only when count **> 3**. Assessor-delayed: latest scheduled assessment per client (global recency dedup), no data jsonb, created ≤ now−6h. DIVERGENCE: web's assessor query is un-scoped (silent 1000-cap); native chunks .in(newIds) — strictly more correct. NOT ported (out of user scope): web's 2 extra urgent alerts (onboarded-without-QHP <12h, first-session-<12h-no-plan) + OnboardingAlertBanner + NewReferredLeadsAlert.

**Dashboard v2 2026-07-16**: layout = greeting → tab strip → animated NEEDS ATTENTION alerts (staggered entrance 90ms apart via useEnterPulse; red = pulsing rail + ping ring, info = glow pulse) → tappable graph cards. Active Clients card + Pullback card → ActiveClientsSheet (web ActiveClientsDialog port: Active-Training/Pullback toggle tiles with web-verbatim labels/hints, trend BarStrip, search client/tier, rows sorted lastSessionAt DESC capped 60, Paused badge PURPLE + pauseReason, days-ago red when ≥3). Revenue card → RevenueSheet fed by NEW hook `useRevenueBreakdown` — RPC `get_super_admin_metric_details {p_metric:'revenue', p_range:'month'}` → only `breakdown` key [{label,value}] consumed (web RevenueBreakdownCard parity; response also has items/total — unused). Stream meta keyed lowercase+trim: renewal=gold/swap, new=blue/userPlus, misc=purple/sparkle, add-on|addon=green/layers, else gray; share % vs analytics revenue total (fallback stream sum). Web's RevenueDetailDialog (Trainer Cost % / Gross Margin economics: useTrainerCostPct, useGrossMargin, trainer_payout_records) NOT ported yet — separate feature if asked.

**Renewals page added 2026-07-16** (route `admin-renewals`, [adminRenewals.tsx](odds-app/src/screens/adminRenewals.tsx), data [adminRenewalQueries.ts](odds-app/src/lib/adminRenewalQueries.ts)) — port of web admin "Sessions" tab (ClientSessionsOverview). Two-phase fetch: useClientBasicInfo (server-paginated 10/page, count exact, search or(first/last ilike), withoutSubscription flips is-null/not-null on subscription_type; package_sessions = latest client_renewals.package_sessions || parseInt(session_package)) then useClientSessionMetrics for visible ids only (consuming sessions = status completed|cancelled|cancelled=true; packageStart = latest renewed_at || created_at; remaining = pkg − after, can go negative). Rows sorted DESC by sessions_after_renewal; row color red ≤0 / gold ≤ floor(30% pkg). client_source hardcoded 'B2C' (web). Generation map: clients generation_admin=true → generation_members[] → member badge + renewal blocked (renewed only by admin). WRITES (web-verbatim, not test-fired): renewal-date = update latest client_renewals.renewed_at else INSERT stub {package_sessions, new_package, cycle_type:'custom'}; renew = insert client_renewals {previous_package: session_package, new_package:'N sessions', package_sessions, cycle_sessions, package_amount?, package_duration(int months), cycle_type, renewed_at:now} + clients.notes = "(Package renewed on <date>. Carried forward X overdue sessions...)" — clients package fields stay FROZEN. NEW tab "Renewal Opportunities" (user-requested, not in web): full scan of active subscribed clients, remaining < 3, sorted most-exhausted; live 2026-07-16: 239 clients, 18 opportunities (8 ≤0).

**Requests page 2026-07-16** (route `admin-requests`, [adminRequests.tsx](odds-app/src/screens/adminRequests.tsx), data [adminRequestQueries.ts](odds-app/src/lib/adminRequestQueries.ts)) — renamed from short-lived "Referrals" page (adminReferrals.tsx DELETED, referral hooks moved out of adminQueries.ts). FOUR tabs with pending-count badges, all approve/reject flows insert `incentive_events` for the requester on approval (insert errors console-logged, not thrown — web parity):
1. **Referrals** (`referrals` + referrer profile join; filters pending/approved/rejected/all + name search; reject reason optional; approve → incentive event_type 'referral'). Live: 39 rows, 5 pending (Ashok agarwal / Khalid Ahmad trainer — screenshot match).
2. **Upgrades** (`client_subscription_history` + client/requester joins, status field = `status`; approve → clients.subscription_type UPDATED to new type + incentive 'subscription_upgrade'; reviewed_at/changed_by stamped). Live: 3 rows, 1 pending (Bhavna Tokas, Odds plus→Odds pro).
3. **Renewals** (`client_renewals`, status field = `request_status` — NULL for legacy rows which are excluded from history; approve → renewed_at stamped now + incentive 'renewal'; card shows previous_package→new_package + sessions/duration-months/₹amount/cycle). Live: 564 rows, 4 pending (Surbhi Gupta 16→32 ₹1,04,000).
4. **Cross-Sell** (`client_packages` .not(request_status,is,null); approve → status='active' + incentive 'cross_sell'; card shows service_name badge + sessions_total + start_date–expiry_date). Live: 6 rows, 5 pending (Jitesh Bajaj, Nutrition Counselling).
Pattern: PENDING APPROVAL section (Approve → ConfirmSheet optional notes; Reject → reason) + HISTORY section (slice 10, admin_notes + reviewed date). All queries mounted at page level for tab badges.

**Requests page expanded to 9 tabs 2026-07-16** (New Leads is DEFAULT): + New Leads (`useAdminNewLeadRows` full rows, same cutoff/self-heal filter; "Add as Client" = minimal ClientFormSheet → clients INSERT **with id = lead.id** (web forcedClientId), status active/client_type B2B, then leads.update {client_id, converted_at, converted_by} — stage untouched — + best-effort edge fn `notify-new-client-card` when QHP Booked; web uses full ClientForm w/ coach/trainer pickers — native is minimal-field, deliberate); + Ref Leads (`referred_leads` table, statuses new/contacted/qualified/qhp_scheduled/converted/rejected, null/''/pending→new normalize, 7 stat tiles, status chip update, Add Client = plain clients insert then status converted); + Renewal Pay (`renewal_payment_requests` excluding request_notes ilike %"kind":"additional_package%; pending = admin_decision='pending_admin' → approve/reject writes {admin_decision, admin_id, admin_notes} ONLY (payment_status/decided_at backend-managed); History slice 20; Mark Paid gate = (cash|bank_transfer) && approved && awaiting_payment → {payment_status paid, paid_at, cash_reference} — package update is a DB trigger); + Invoice Raised (Pending = useInvoiceRaisedRows; Processing/Done from lead_invoice requests split by payment_status [awaiting_approval|awaiting_payment|failed] vs paid; Generate Payment = insert admin-approved awaiting_payment row w/ request_notes kind:lead_invoice + Odds-generation special-case sets client generation_admin/members + razorpay invokes edge fn `create-lead-razorpay-link` — failure flips row to 'failed' w/ Retry; cash mark-paid uses cash_reference 'Marked received by admin'); + Paid Cancel (session_schedule paid_cancellation=true, pending/all filter on admin_approval; APPROVE = INSERT training_sessions {status cancelled, cancelled true, paid_cancellation true, schedule_session_id} THEN update admin_approval approved; reject = update only). Live-verified: 6 raw new leads, 2 referred, 34 lead-invoice reqs (1 processing/27 done), 1 paid-cancel pending, Sana paid/bank_transfer — all screenshot matches.

**Incidents page 2026-07-16** (route `admin-incidents`, [adminIncidents.tsx](odds-app/src/screens/adminIncidents.tsx), data [adminIncidentQueries.ts](odds-app/src/lib/adminIncidentQueries.ts)) — web TrainerIncidentsPanel port. Table `trainers_incidents` {trainer_id, author_id, author_role 'admin'|'crm', message, created_at}. Trainer list = ALL profiles role='trainer' paginated (82 live), search by name, count badge = incidents BY CURRENT AUTHOR only (web quirk — .eq author_id me, not total). Open → sheet: incident log desc (author name via manual profiles join — fkey join `trainers_incidents_author_id_fkey` exists on web but manual is safer) + composer INSERT {trainer_id, author_id, author_role:'admin', message min 5 chars}. Verified live: 82 trainers, 3 incidents exist (latest by crm "testing"), admin has 0 authored.

**Messenger for admin 2026-07-16**: shared Messenger screen (route 'messenger') is role-agnostic and was already in adminNav; fixed its `homeRoute` (messenger.tsx ~1086) which hardcoded crm/trainer dashboards — now maps coach/ops/admin to their own dashboards too.

**Clients page 2026-07-16** (route `admin-clients`, [adminClients.tsx](odds-app/src/screens/adminClients.tsx), data [adminClientQueries.ts](odds-app/src/lib/adminClientQueries.ts)) — web /admin/clients port over VIEW `unified_clients` (cols verified live incl. profile_id/client_source/is_odds_converted). Filter pipeline verbatim: type all/b2b/b2c(+is_odds_converted=false)/odds_converted/paused (two-step: client_pause_history is_active ids → .in); status tabs active(sub NOT null)/without_subscription(sub null)/inactive/discontinued; TAB COUNTS are keyed by clientFilter and apply ONLY b2b/b2c/odds_converted branches (web ignores paused+subscription there; Active tab has NO count) — verified: odds_converted → 4/4/15 exact screenshot match, unfiltered → 241/16/166; sub filter none|exact from SUBSCRIPTION_OPTIONS. Search (800ms debounce) = or(first/last/email ilike), unpaginated; else 30/page created_at desc. Enrichment per page: completed-session counts, actively_training trainer names (profiles join), paused set. Mutations: delete B2B-only (trainer_clients then clients), toggle ODDS + subscription change DUAL-WRITE clients+profiles when B2C profile_id, reactivate (status active + best-effort client_status_history insert in try/catch — column names guessed prev/new_status/changed_by). NOT ported: ConvertB2BClientsDialog bulk flow, copy-credentials, client detail page (card sheet has the actions instead).

**Client detail page 2026-07-16** (route `admin-client-detail`, [adminClientDetail.tsx](odds-app/src/screens/adminClientDetail.tsx), data [adminClientDetailQueries.ts](odds-app/src/lib/adminClientDetailQueries.ts)) — opened from Clients card tap (selectedClientId via store; card's ⋯ button keeps the quick-action sheet). Hero: status Switch (clients.status + best-effort client_status_history), badges, KPI strip (onboarded/package X÷Y/total sessions/type) + package progress ("Renewal Pending" red when remaining ≤0; SIMPLIFIED cycle math — latest client_renewals boundary, no shared-pool/Odds-Generation arithmetic). Pause banner from client_pause_history is_active. Tabs: Overview (client info; subscription+ODDS reuse list mutations' dual-write; trainers card = ALL trainer_clients assignments w/ roles normalized others→doctor, actively_training Switch, 2-tap remove; + Assign opens AssignSheet (web AssignTrainerDialog UI): currently-active gold chips + sectioned checklists Trainers/Doctors/CRMs (roster roles trainer/doctor/others/crm), check=assign insert / uncheck=remove row, search filter), Sessions (read-only upcoming/past 40 rows — web's session CRUD/±60min conflict rules NOT ported), Remarks (client_remarks {client_id, author_id, content} verified cols; add + list). **Detail v2 2026-07-16**: + Monthly switch (clients.is_monthly_subscription), Pause button → client_pause_history insert {pause_start/end, reason_admin, is_active, paused_by} + End-pause (is_active=false); 5 tabs — Sessions tab rebuilt as Training/Workout sub-tabs: Training = package/cycle tree (useSessionsByCycle port: P1 total = renewals[0].previous_package || session_package, renewal N → pkg N+1 w/ cycle_sessions; countable = completed+cancelled excl complimentary; physio/RLT on 'odds basic' → Other; NOT ported: reset-window gating, generation shared pool) + Add/Edit/Delete session (useSaveSession: cancelled→cancelled=true, attendance_marked always true; no attachments/AI/unique/±60min-conflict) + gift complimentary toggle; Workout = workout_exercises deduped by session_id. Goals tab = daily_goals last 4 weeks read-only (sleep_target_hours/steps_target/z2c_target/nutrition_target/recommendation). Odds Generation tab = generation_admin switch + members add(search)/remove writing clients.generation_members + enrollment date. **Detail v3 2026-07-16**: Session sheet gained web "Session Focus" chips (SESSION_FOCUS_OPTIONS verbatim: strength/mobility/endurance/hiit/rehabilitation/assessment/physiotherapy/massage_therapy/red_light_therapy/cold_bath_therapy); header Pause↔Resume always visible; + Additional Package button → useCreateAdditionalPackage FULL web port (services EXACTLY `reset` + `nutrition_package`; Reset form = sessions* + free-text duration*; Nutrition form = months* only with package_sessions NULL + nutrition_duration; amount required ONLY for razorpay) (additional_packages insert w/ created_by + payment_date null → tagged renewal_payment_requests {kind:additional_package, new_cycle_type 'Custom', admin_decision approved, awaiting_payment} → razorpay edge fn `create-additional-package-razorpay-link`, orphan pkg row rolled back on failure; native shows payment link selectable). STILL not ported: Credentials/Update Credentials (auth/password flows), Training Partner toggle, Referral Code, Reset/Nutrition sub-tabs, session remarks/attachments/XLSX, ±60min conflict rule.

**Users page 2026-07-16** (route `admin-users`, [adminUsers.tsx](odds-app/src/screens/adminUsers.tsx), data [adminUserQueries.ts](odds-app/src/lib/adminUserQueries.ts)) — web /admin/users port; views = Users | Create User | Assignments. Create User is ONE merged form for all roles (role dropdown inside the form, doctor specialization appears conditionally); Users view has a "Create <Role>" shortcut that preselects the role. Roles: coach/trainer/doctor/crm/marketing/academy/super_admin/ops. Create → edge fn `admin-create-user` {email,password,first_name,last_name,role,doctor_specialization_tag} (GOTCHA: existing email → fn silently updates that user's role); edit → edge fn `admin-update-user` {userId,email?,password?} (admin-gated via user_roles server-side, syncs profiles/clients email). Doctor specialization: physiotherapist|nutritionist chips inline (profiles.doctor_specialization_tag). Delete = web guards (coach: coach_trainers; trainer: training_sessions + coach_trainers + active trainer_clients) then profiles.delete — auth.users row stays orphaned (web gotcha, client-side admin.deleteUser no-ops). Assignments view: coach_trainers with SINGLE joined read `coach:profiles!coach_id(...)` (verified live — fixes web N+1), coaches=role coach|doctor, trainers=trainer|doctor, dup pre-check + self-assign block. Live counts: coach 1/trainer 82/doctor 10/crm 5/marketing 2/academy 1/super_admin 1/ops 2. Edge fns NOT test-fired (create real users).

**Performance page 2026-07-16** (route `admin-performance`, [adminPerformance.tsx](odds-app/src/screens/adminPerformance.tsx), data [adminPerformanceQueries.ts](odds-app/src/lib/adminPerformanceQueries.ts)) — 2 tabs. CRM = web useIncentiveLeaderboard port: approved `referrals` (per referrer_id) + `incentive_events` cross_sell/package_upgrade/subscription_upgrade (per user_id, filtered on **event_date** not created_at for month), every role='crm' profile seeded at 0, points = plain sum, This Month = local startOfMonth; 4 stat tiles = column sums. Trainer = web TrainerPerformanceTable admin view: completed + attendance_marked sessions, ROLLING windows now−7d vs now−14d..−7d (not calendar weeks), pct rules (0→N =+100, N→0 =−100, −0 normalized), sorted by current week, top-10 + show-all. Live: 36 incentive events (31 referral/4 sub-upgrade/1 cross-sell), 28 approved referrals, 385 completed sessions last 7d.

**Certifications page 2026-07-16** (route `admin-certifications`, [adminCertifications.tsx](odds-app/src/screens/adminCertifications.tsx), data [adminCertQueries.ts](odds-app/src/lib/adminCertQueries.ts)) — table `odds_certifications` + `profiles:trainer_id` embed (11 rows live). WEB QUIRK ported for parity: displayed overall % = WEIGHTED sum/106, stored grade = UNWEIGHTED avg of 3 sub-scores (A≥90/B≥80/C≥70/D≥60/F) — same cert can show 88% + grade A. Scores clamped 0-90/0-6/0-10. Native FIXES vs web: FK-safe name render, clean update payload (web leaked whole object incl. joined profiles). Form: searchable trainer picker (role='trainer' only), live unweighted preview + grade pill. Delete = plain row delete (no FK guards needed).

**Tools + Trainer Fees 2026-07-16** (routes `admin-tools` hub card → `admin-trainer-fees`, single file [adminTools.tsx](odds-app/src/screens/adminTools.tsx) with hooks inline) — table `trainer_fees` {trainer_id, client_id, fee_amount, payout_method monthly|fortnightly|advance}, embeds via NAMED fkeys `trainer_fees_trainer_id_fkey`/`trainer_fees_client_id_fkey` (verified live: 455 rows, Tabassum→Shantanu ₹800 monthly). Trainer picker deliberately over-broad (roles trainer/doctor/coach/crm — web design); client picker = ALL clients with non-active status chip. EDIT only updates {fee_amount, payout_method} — pair immutable, delete+re-add to move (web rule). Native adds an amount>0 guard the web lacks; NO duplicate-pair guard (web parity — downstream payout math double-counts dupes). Filter chips = trainers having fee rows only. Fees feed useTrainerCostPct/payout calculators everywhere. Second tool card **Manage Managers & Team** (route `admin-manage-teams`, same file) — table `manager_score` {manager_id, team_name, team_json = member ids[], team_start, team_end null=ongoing, total_sessions/winner written by LEADERBOARD pipeline not this page} (verified live: team "Sagar" 2026-05-01→07-31, 9 members). Rules ported: 1-10 team batch w/ profile-unique-per-batch (client-side only), hasOngoingPeriod (team_end null or ≥ today, ISO string compare) locks CREATE but not edit; edit = team_name/manager_id/team_json only, availability vs same-period teams; manager pool = trainer/doctor/coach/crm.

**Tools batch 2 2026-07-16**: + Churn Requests (route `admin-churn`, [adminChurn.tsx](odds-app/src/screens/adminChurn.tsx)) — client_discontinuation_requests w/ clients embed + BATCHED profile lookup (fixes web N+1); approve = update request → clients.status discontinued → best-effort client_status_history (web 3-step, no txn); reactivate similar; tabs pending/processed/discontinued. GOTCHA: clients table has NO discontinuation_reason/details/discontinued_date columns — the Discontinued tab derives them from the latest client_status_history row with new_status='discontinued' (166 live). + Revenue Tracker (`admin-revenue-tracker`) & Revenue Summary (`admin-revenue-summary`) in [adminRevenue.tsx](odds-app/src/screens/adminRevenue.tsx) — BOTH behind password gate via edge fn `verify-revenue-tracker-password` (verified live: wrong pw → {ok:false}); unlock held in module memory per session. Tracker (scoped): clients status∈active/inactive/discontinued minus staff-sub, latest non-rejected renewal (asc order last-write), LTV = client.package_amount + Σ renewals, missing-amount filter; NOT ported: charts, 7 tabs, inline-edit RPCs update_package_amount/update_payment_date, pace-based expiry, forecast. Summary: month KPIs (newClient/renewal[skip rejected]/addons incl additional_service_renewals/misc from clients.miscellaneous_payments jsonb) via payment_date.slice(0,7); Pending KPI = renewal_payment_requests NOT paid/cancelled GLOBAL (web parity, not month-scoped); ledger merged+sorted; NOT ported: margins/opex CRUD, invoice editor.

**Gotcha (react-query v5 + offline)**: this app pauses queries via NetInfo onlineManager — offline-paused queries have isLoading=false with data undefined. Gate loading UI on **isPending**, or empty-state branches ("All clear") render falsely while offline. All useAdminUrgentAlerts sub-queries/RPCs THROW on error — swallowing one chunk fabricates counts (e.g. everyone flagged trainer-less).

---
name: ops-workspace
description: "Native Ops (sales/operations) workspace — file map, backend contracts, RPC guards, role plumbing, gotchas"
metadata: 
  node_type: memory
  type: project
  originSessionId: 5527b21c-0de8-4869-8f72-0c35b4727fce
---

**Ops workspace built 2026-07-16** in odds-app (port of web /ops/*). Data layers: [opsLeadQueries.ts](odds-app/src/lib/opsLeadQueries.ts) (leads pipeline), [opsQueries.ts](odds-app/src/lib/opsQueries.ts) (targets/holds/CRM-pending/paid-clients/baseline), [opsEscalationQueries.ts](odds-app/src/lib/opsEscalationQueries.ts). Screens: [ops.tsx](odds-app/src/screens/ops.tsx) (OpsDashboard, OpsClients, OpsCrmPending, OpsCrmActivity, OpsBaseline, OpsTargets, OpsQhpHold), [opsLeads.tsx](odds-app/src/screens/opsLeads.tsx) (list + filters + LeadFormSheet + LeadSheet + StageFlowSheet with all 8 web dialogs), [opsEscalations.tsx](odds-app/src/screens/opsEscalations.tsx) (8 categories + derived Renewals Pending). QHP Stats reuses trainer route `qhp-stats` (chrome itemVisible bypasses the QHP-manager gate when role==='ops').

**Role plumbing** (recipe reused from [[coach-workspace]]): Role union in store.tsx + appRoleOf in auth.tsx (`dbRole==='ops'→'ops'`) + opsNav in data.ts + drawer pick in chrome.tsx + 9 `ops-*` routes in Router.tsx + both post-login redirects (Router session gate AND SignIn.doSignIn) + PREFILL/ROLE_OPTS in trainer.tsx (ops email prefilled, **no ops password on file** — ops@oddsfitness.com = Sunaina Sethia, preeti@oddsfitness.com = Preeti Choudhury).

**Stage-change interception** (web LeadsTable port, in LeadSheet.changeStage): Potential→qualify dialog (qualified_lead_criteria jsonb, 7 criteria + fitness_goals[] + completed_at); Converted→package dialog if lead.client_id (clients update FIRST, then lead) else date-only; QHP Booked→direct update if prefs already set, else booking dialog (writes lead qhp_pref_* fields, NOT qhp_schedule); Follow Up→schedule dialog (supersede pending entries); Lost→required remark (appends to remarks[] + stage in one patch); Raise invoice→invoice_details jsonb (amount/duration/sessions_in_package/cycle:'Monthly'/subscription_type/raised_at/generation_* nulls). Other stages update directly.

**RPC guards verified live 2026-07-16** (as CRM, bogus UUIDs): `toggle_lead_spam(_lead_id)` → 42501 "Not allowed to update spam status" (server enforces canMarkLeadSpam); `add_qhp_hold_reply(_lead_id,_message)` → P0001 "Hold not found"; `append_sales_target_note(p_target_id,p_category,p_note)` → P0001 "Only Ops staff can add coaching notes" (**ops-only** — CRM/coach get errors, so target notes are untestable without an ops login). `get_cold_lead_ids(_days:5)`=378 ids, `get_qhp_holds()` verified earlier.

**Dashboard revamped 2026-07-16** after user reported broken UI. ROOT CAUSE was a shared-primitive bug: `Card` (primitives.tsx) with `onPress` copied width/flex layout styles onto the Pressable wrapper but ALSO left them on the inner LinearGradient, so `width: '47.5%'` applied twice → tiles rendered at ~22% of the row. Only the ops dashboard combined onPress + percentage width, so only it looked broken. Fixed in primitives.tsx (layout keys stripped from inner, `flexGrow: 1` added so flex-stretched press cards still fill). New OpsDashboard: GreetingHeader, animated OpsAlertBanner (usePulse, preview rows), hero pipeline card (CountUp + conversion ProgressBar + mini-stat strip), KPI pair, NEEDS ATTENTION queue rows with explicit loading/error/all-clear states (error must NOT render "All clear" — data ?? [] masks isError), up-next follow-ups list, 6 workspace tiles.

**Follow-up reminder panel added 2026-07-16** (FollowUpReminderPanel in opsLeads.tsx, port of web OpsFollowUpNotificationBanner): expandable card at top of Leads — counts "X overdue · Y due in next 24h" (segments hidden when 0), red when any overdue else gold, hidden entirely when no reminders, 8-card cap + "+N more" footer, static "Notifications: On" chip (server cron `fire-ops-followup-reminders` pushes to lead OWNER, not toggleable). WEB PARITY QUIRKS ported deliberately: stage dropdown on cards is a DIRECT update (bypasses all interception dialogs AND canEditLead); "Add remark" text is only persisted when Mark-as-Done fires (passed as manualRemark). `useCompleteFollowUp` rewritten to web's read-then-write: input is `{leadId, entryId?, manualRemark?, profile}` (NOT lead object), re-reads follow_ups/remarks fresh inside the mutation, flips entry ONLY if still status='pending' (never re-stamps completed/superseded audit), persisted remark label must byte-match date-fns `dd-MMM-yyyy h:mm a` IST ("13-Jul-2026 2:30 PM") — built via Intl formatToParts, falls back to now.

**Gotchas**: OpsDashboard passes quick-filters to OpsLeads via `set({crmSection})` (reused store field), consumed once in an OpsLeads effect. Escalations only count rows since ESCALATIONS_SINCE='2026-06-01T00:00:00+05:30'; per-category count modes differ (tier2 / tier2_level2 / tier2_level2plus / all). Renewals Pending is fully client-side derived (active clients → latest approved client_renewals → consumed training_sessions ≥ package, exhausted 24h+ ago, no newer renewal). Baseline explanation dual-source cutoff BASELINE_CUTOFF_MS=2026-05-28T23:59:59+05:30.
